## Mobile app Be the hero

> App build with React Native

- Install all dependencies `yarn` or `npm install`;
- Build android app and install in device `yarn android` or `npm run android`;
- Run App `yarn start` or `npm start`;